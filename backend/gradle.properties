# Local build performance: reuse task outputs, cache the configuration phase,
# and run independent tasks in parallel. Safe to keep enabled in CI as well.
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.parallel=true
