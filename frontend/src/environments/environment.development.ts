/**
 * Development environment — replaces `environment.ts` during `ng serve` and
 * development builds (see the `fileReplacements` entry in angular.json).
 *
 * See environment.ts for notes on `primengLicense`.
 */
export const environment = {
  production: false,
  primengLicense: '',
};
