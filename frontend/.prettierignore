# Build output & caches
/dist
/tmp
/out-tsc
/.angular

# Dependencies
/node_modules

# Lockfiles (managed by the package manager)
pnpm-lock.yaml

# Coverage
/coverage

# Markdown — left to the author; respects .editorconfig (preserve trailing
# whitespace and line length, which matter for Markdown line breaks)
*.md

